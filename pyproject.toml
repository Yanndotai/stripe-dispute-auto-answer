[project]
name = "dispute-automation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "stripe>=9.0.0",
    "requests>=2.31.0",
    "fpdf2>=2.7.0",
    "openai>=1.40.0",
]

[tool.uv]
package = false

[tool.vercel]
entrypoint = "api.stripe_webhook:handler"
